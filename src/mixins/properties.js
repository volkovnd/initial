export const variant = () => ({
  type: String,
  default: null,
});

export const bgVariant = () => ({
  type: String,
  default: null,
});

export const textVariant = () => ({
  type: String,
  default: null,
});

export const size = () => ({
  type: String,
  default: null,
});

export const col = () => ({
  type: String,
  default: null,
});

export const column = () => ({
  type: Boolean,
  default: false,
});

export const fluid = () => ({
  type: Boolean,
  default: false,
});

export const active = () => ({
  type: Boolean,
  default: false,
});

export const tagName = () => ({
  type: String,
  default: "div",
});
