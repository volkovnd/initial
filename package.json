{
  "name": "@volkovnd/initial-new",
  "private": true,
  "description": "My initial project",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "lint:javascript": "eslint . --cache --fix",
    "lint:styles": "stylelint '**/*.{css,scss}' --fix",
    "lint": "run-p lint:*",
    "pretest": "npm run lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "posttest": "npm run format",
    "format": "prettier --loglevel warn --write \"**/*.{js,json,css,scss,md,html}\"",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "Volkov Nikolay <volkovnd@yandex.ru> (https://github.com/volkovnd/)",
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.4.1",
    "rimraf": "^3.0.2",
    "stylelint": "^14.1.0",
    "stylelint-config-prettier": "^9.0.3",
    "stylelint-config-recess-order": "^3.0.0",
    "stylelint-config-standard": "^24.0.0",
    "stylelint-config-standard-scss": "^2.0.1"
  }
}
